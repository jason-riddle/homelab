---
all:
  hosts:
    drupal.local:
      ansible_host: 192.168.1.170
      ansible_port: 22
      ansible_user: pi

  children:
    drupal:
      hosts:
        drupal.local:

    # home-assistant:
    #   hosts:
    #     home-assistant.local:
